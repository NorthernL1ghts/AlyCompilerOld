; a : integer

;[integer (z : integer) { z }]

;x : integer

defun foo (x : integer) : integer {
    bar : function = [integer (y : integer) { y }]
    x
}

; TODO: Support binary operator (re)definition
; _ : operator = (x : integer, y: integer) {
;
; }

; 2 * 3 / 5 + 1 / 5
; TODO: Something breaks here :^(

2 * 3 / 2 + 1

; 2 * 3 + 1 + 2 + foo(5)